"""Legacy tests for the previous backend.engine WHOIS adapter.

The refactored project uses backend/utils/whois_utils.py and domain heuristics.
"""

import pytest

pytest.skip("Legacy engine tests removed after refactor", allow_module_level=True)
